#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for linting and formatting
npx lint-staged

# Run tests to ensure email functionality works
npm test -- --testPathPatterns=emailService
